// This file is obsolete.
// The page is now handled by /src/app/[locale]/(main)/dashboard/page.tsx
// It can be deleted, but we'll keep it empty for now to avoid breaking references.
export default function ObsoleteDashboardPage() {
  return null;
}
