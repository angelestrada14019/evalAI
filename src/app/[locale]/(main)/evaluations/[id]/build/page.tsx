// This file is obsolete.
// The page is now handled by /src/app/[locale]/(main)/evaluations/[id]/(builder)/build/page.tsx
// It can be deleted, but we'll keep it empty for now to avoid breaking references.
export default function ObsoleteBuilderPage() {
  return null;
}
